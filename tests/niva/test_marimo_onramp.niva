# ============================================================================
# Companion to tests/test_marimo_onramp.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for niva.setup.marimo — the optional marimo on-ramp (planning doc 21 §10).

# ── TestPreflight ───────────────────────────────────────────────

# test_not_in_qgis
#   (pure Python — no niva flow)

# test_below_qgis_4
#   (pure Python — no niva flow)

# test_ok_returns_none
#   (pure Python — no niva flow)

# ── TestDetection ───────────────────────────────────────────────

# test_find_plugin_dir
#   (pure Python — no niva flow)

# test_is_installed
#   (pure Python — no niva flow)

# ── TestDownloadRelease ─────────────────────────────────────────

# test_downloads_to_dest
#   (pure Python — no niva flow)

# test_download_failure_returns_message
#   (pure Python — no niva flow)

# ── TestInstallFromZip ──────────────────────────────────────────

# test_uses_qgis_plugin_installer
#   (pure Python — no niva flow)

# test_returns_false_without_qgis
#   (pure Python — no niva flow)

# ── TestOrchestration ───────────────────────────────────────────

# test_dry_run_reports_without_acting
#   (pure Python — no niva flow)

# test_already_installed_just_starts_marimo
#   (pure Python — no niva flow)

# test_absent_downloads_installs_then_starts
#   (pure Python — no niva flow)

# test_download_failure_aborts
#   (pure Python — no niva flow)
