# ============================================================================
# Companion to tests/test_color.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for the zero-dependency ANSI colour helper (`niva.color`). Pure Python.

# ── TestColor ───────────────────────────────────────────────────

# test_no_color_disables_even_with_force_always
#   (pure Python — no niva flow)

# test_force_always_wraps_in_ansi
#   (pure Python — no niva flow)

# test_force_never_disables
#   (pure Python — no niva flow)

# test_paint_without_styles_is_plain
#   (pure Python — no niva flow)
