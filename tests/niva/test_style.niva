# ============================================================================
# Companion to tests/test_style.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# `style` verb — apply/save a `.qml` style or `.qmd` metadata sidecar. Mock-backed

# ── TestStyleVerb ───────────────────────────────────────────────

# test_save_records
#   (pure Python — no niva flow)

# test_apply_records
#   (pure Python — no niva flow)

# test_qmd_metadata_extension_accepted
#   (pure Python — no niva flow)

# test_is_pass_through
apply "{self.qml}" | save "{self.tmp}/o.gpkg"

# test_bad_action_is_error
#   (pure Python — no niva flow)

# test_bad_extension_is_error
#   (pure Python — no niva flow)

# test_apply_missing_file_is_error
#   (pure Python — no niva flow)

# test_needs_a_loaded_layer
style save "{self.tmp}/o.qml"

# test_wrong_arity_is_error
#   (pure Python — no niva flow)

# test_save_sld_and_qlr
#   (pure Python — no niva flow)

# test_apply_rejects_export_only_formats
#   (pure Python — no niva flow)
