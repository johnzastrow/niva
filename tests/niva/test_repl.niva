# ============================================================================
# Companion to tests/test_repl.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for the repl's manifest-driven tab completion (`niva.cli.repl.completions`). Pure

# ── TestReplCompletion ──────────────────────────────────────────

# test_stage_start_completes_verb_names
#   (pure Python — no niva flow)

# test_stage_start_includes_builtins
#   (pure Python — no niva flow)

# test_after_pipe_completes_verbs
load a.gpkg | buf

# test_after_verb_completes_its_options_and_flags
#   (pure Python — no niva flow)

# test_option_prefix_filters
#   (pure Python — no niva flow)

# test_enum_option_completes_values
#   (pure Python — no niva flow)

# test_unknown_verb_offers_nothing
#   (pure Python — no niva flow)

# ── TestReplCommands ────────────────────────────────────────────

# test_quit_variants_all_quit
#   (pure Python — no niva flow)

# test_help_variants_print_help
#   (pure Python — no niva flow)

# test_mistyped_dot_command_is_flagged_not_parsed
#   (pure Python — no niva flow)

# test_flow_line_is_validated_and_remembered
load a.gpkg | save b.gpkg

# ── TestHighlight ───────────────────────────────────────────────

# test_classify
#   (pure Python — no niva flow)

# test_highlight_flow_is_transparent_without_color
load a.gpkg | buffer 100m | save b.gpkg
