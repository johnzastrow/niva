# ============================================================================
# Companion to tests/test_setup.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for the setup-core (planning doc 21).

# ── TestPurePathLogic ───────────────────────────────────────────

# test_append_to_empty
#   (pure Python — no niva flow)

# test_append_adds_at_end
#   (pure Python — no niva flow)

# test_append_idempotent
#   (pure Python — no niva flow)

# test_append_preserves_existing_verbatim
#   (pure Python — no niva flow)

# test_append_no_double_separator
#   (pure Python — no niva flow)

# test_contains_case_insensitive_on_windows
#   (pure Python — no niva flow)

# test_remove
#   (pure Python — no niva flow)

# test_remove_absent_is_noop
#   (pure Python — no niva flow)

# ── TestLauncherBody ────────────────────────────────────────────

# test_windows_forwards_to_module
#   (pure Python — no niva flow)

# test_posix_forwards_to_module
#   (pure Python — no niva flow)

# test_write_launcher_idempotent
#   (pure Python — no niva flow)

# ── TestDetection ───────────────────────────────────────────────

# test_launcher_target_shape
#   (pure Python — no niva flow)

# test_detect_environment
#   (pure Python — no niva flow)

# ── TestOrchestration ───────────────────────────────────────────

# test_dry_run_writes_nothing
#   (pure Python — no niva flow)

# test_install_refuses_without_qgis_launcher
#   (pure Python — no niva flow)

# test_install_idempotent_then_uninstall
#   (pure Python — no niva flow)

# ── TestPosixRc ─────────────────────────────────────────────────

# test_append_then_remove_roundtrip
#   (pure Python — no niva flow)
