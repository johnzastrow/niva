# ============================================================================
# Companion to tests/test_catalog.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Offline algorithm-catalog tests — `describe <id>` and `run` validation with NO QGIS

# ── TestCatalog ─────────────────────────────────────────────────

# test_catalog_is_packaged_and_populated
#   (pure Python — no niva flow)

# test_param_names_and_defaults_present
#   (pure Python — no niva flow)

# test_unknown_id_is_none
#   (pure Python — no niva flow)

# ── TestDescribeOffline ─────────────────────────────────────────

# test_describe_algorithm_offline
describe native:buffer

# test_describe_pdal_offline
describe pdal:exportraster

# test_describe_unknown_id_offline_errors_clearly
describe native:definitely-not-real

# ── TestRunValidation ───────────────────────────────────────────

# test_good_params_no_warning
#   (pure Python — no niva flow)

# test_unknown_param_warns_with_suggestion
#   (pure Python — no niva flow)

# test_unknown_id_warns
#   (pure Python — no niva flow)

# test_harness_ids_are_skipped
#   (pure Python — no niva flow)

# ── TestExplainVerbValidation ───────────────────────────────────

# test_invented_verb_is_flagged
load a.gpkg | compute foo="1" | save b.gpkg

# test_typo_gets_suggestion
load a.gpkg | reproj EPSG:3857 | save b.gpkg

# test_valid_flow_passes
load a.gpkg | filter "x>1" | buffer 100m | save b.gpkg

# test_run_id_not_treated_as_unknown_verb
load a.las | run pdalcli:to_raster attribute=Z | save b.tif
