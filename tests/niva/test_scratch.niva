# ============================================================================
# Companion to tests/test_scratch.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Raster scratch relocation + cleanup (no QGIS needed).

# ── TestScratchDir ──────────────────────────────────────────────

# test_falls_back_to_system_temp_when_unset
#   (pure Python — no niva flow)

# test_honours_niva_tmpdir_and_points_gdal_at_it
#   (pure Python — no niva flow)

# test_unwritable_niva_tmpdir_falls_back
#   (pure Python — no niva flow)

# ── TestPurgeScratch ────────────────────────────────────────────

# test_purges_intermediates_but_spares_the_final_layer
#   (pure Python — no niva flow)

# test_keep_none_purges_everything
#   (pure Python — no niva flow)

# test_missing_file_is_not_an_error
#   (pure Python — no niva flow)

# test_remove_dir_drops_empty_niva_scratch_dir_on_success
#   (pure Python — no niva flow)

# test_remove_dir_never_touches_a_nonempty_or_system_dir
#   (pure Python — no niva flow)

# test_remove_dir_no_op_without_niva_tmpdir
#   (pure Python — no niva flow)

# test_temp_path_is_tracked_so_it_gets_purged
#   (pure Python — no niva flow)

# ── TestCommandFailureDetection ─────────────────────────────────

# test_detects_nonzero_gdal_exit
#   (pure Python — no niva flow)

# test_ignores_zero_exit_and_plain_errors
#   (pure Python — no niva flow)

# test_raise_on_command_failure_raises_for_nonzero
#   (pure Python — no niva flow)

# test_raise_on_command_failure_is_quiet_when_clean
#   (pure Python — no niva flow)
