# ============================================================================
# Companion to tests/test_find.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for `niva find` (issue #43). The offline layer — walking, base records, both filter

# ── TestParsers ─────────────────────────────────────────────────

# test_parse_size
#   (pure Python — no niva flow)

# test_parse_age
#   (pure Python — no niva flow)

# test_human_size
#   (pure Python — no niva flow)

# test_exts_for_pattern
#   (pure Python — no niva flow)

# ── TestScanAndBase ─────────────────────────────────────────────

# test_walk_recursive
#   (pure Python — no niva flow)

# test_walk_shallow
#   (pure Python — no niva flow)

# test_walk_max_depth
#   (pure Python — no niva flow)

# test_walk_single_file
#   (pure Python — no niva flow)

# test_base_record
#   (pure Python — no niva flow)

# ── TestFilters ─────────────────────────────────────────────────

# test_match_base_pattern_and_exts
#   (pure Python — no niva flow)

# test_match_base_size_and_format
#   (pure Python — no niva flow)

# test_match_base_newer_than
#   (pure Python — no niva flow)

# test_match_meta
#   (pure Python — no niva flow)

# test_match_meta_missing_probe_fails_feature_filter
#   (pure Python — no niva flow)

# ── TestFindAndFormat ───────────────────────────────────────────

# test_find_defaults_to_spatial_only
#   (pure Python — no niva flow)

# test_find_pattern_and_limit
#   (pure Python — no niva flow)

# test_format_table_empty_and_nonempty
#   (pure Python — no niva flow)

# test_format_json_roundtrips
#   (pure Python — no niva flow)

# test_format_as_flow_emits_valid_each_lines
| save

# test_format_paths_is_bare_and_pipeable
#   (pure Python — no niva flow)

# ── TestEnrichment ──────────────────────────────────────────────

# test_enrich_vector_surfaces_fields_and_fid
#   (pure Python — no niva flow)
