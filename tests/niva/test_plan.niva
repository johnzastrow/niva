# ============================================================================
# Companion to tests/test_plan.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for the resolved-plan IR (`niva.plan`, docs/planning/20 §3). Pure Python, no QGIS.

# ── TestPlanShape ───────────────────────────────────────────────

# test_top_level_fields
load a.gpkg | buffer 100m | save b.gpkg

# test_is_json_serializable
load a.gpkg | reproject EPSG:6346 | buffer 100m dissolve | save b.gpkg

# test_step_ids_are_sequential
load a.gpkg | buffer 100m | save b.gpkg

# ── TestResolution ──────────────────────────────────────────────

# test_alias_resolves_to_algorithm
load a.gpkg | buffer 100m dissolve | save b.gpkg

# test_distance_is_value_unit
load a.gpkg | buffer 100m | save b.gpkg

# test_injected_defaults_captured
load a.gpkg | buffer 100m | save b.gpkg

# test_user_param_is_not_injected
load a.gpkg | buffer 100m segments=12 | save b.gpkg

# test_run_id_passthrough
load t.las | run pdal:exportraster ATTRIBUTE=Z RESOLUTION=1 | save r.tif

# test_builtin_has_no_algorithm
load a.gpkg | buffer 100m | save b.gpkg

# ── TestDataFlowAndDiagnostics ──────────────────────────────────

# test_linear_inputs
load a.gpkg | buffer 100m | save b.gpkg

# test_diagnostics_are_carried
load a.gpkg | compute x=1 | save b.gpkg

# test_requires_qgis_true_for_data_flow
load a.gpkg | buffer 100m | save b.gpkg

# test_requires_qgis_false_for_pure_offline
#   (pure Python — no niva flow)

# ── TestFormatPlan ──────────────────────────────────────────────

# test_renders_op_and_algorithm
load a.gpkg | buffer 100m dissolve | save b.gpkg

# test_distance_is_human_not_dict
load a.gpkg | buffer 100m | save b.gpkg

# test_injected_defaults_are_labeled
load a.gpkg | buffer 100m | save b.gpkg

# test_diagnostics_surface_errors
load a.gpkg | compute x=1 | save b.gpkg

# test_clean_plan_reports_no_diagnostics
load a.gpkg | buffer 100m | save b.gpkg

# test_header_shows_requires_qgis
load a.gpkg | buffer 100m | save b.gpkg
