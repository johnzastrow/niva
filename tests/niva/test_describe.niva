# ============================================================================
# Companion to tests/test_describe.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# `describe` tests (planning 11). The verb path is pure (no QGIS); the algorithm

# ── TestDescribeVerb ────────────────────────────────────────────

# test_every_builtin_verb_is_describable
#   (pure Python — no niva flow)

# test_describe_map_and_figure
describe map
describe figure

# test_describe_alias_shows_mapping
describe buffer

# test_describe_join_required_option
describe join

# test_unknown_name_is_flowerror
describe frobnicate

# test_curated_example_is_shown
describe buffer
load roads.gpkg | buffer 100m dissolve

# test_synthesized_example_when_no_curated
describe vertices
load roads.gpkg | vertices | save out.gpkg

# test_describe_builtin_verb
describe save

# ── TestExamplesActuallyRun ─────────────────────────────────────

# test_every_alias_example_parses_and_executes
#   (pure Python — no niva flow)

# test_every_builtin_example_parses
#   (pure Python — no niva flow)

# test_algorithm_example_is_synthesized_and_runs
#   (pure Python — no niva flow)
