# ============================================================================
# Companion to tests/test_manifest.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for the machine-readable manifest + synonym map (`niva.manifest`,

# ── TestManifest ────────────────────────────────────────────────

# test_shape
#   (pure Python — no niva flow)

# test_is_json_serializable
#   (pure Python — no niva flow)

# test_verb_entry_has_params_defaults_enums
#   (pure Python — no niva flow)

# test_synonyms_attached
#   (pure Python — no niva flow)

# test_verb_without_synonyms_is_empty_list
#   (pure Python — no niva flow)

# ── TestSynonyms ────────────────────────────────────────────────

# test_map_loads
#   (pure Python — no niva flow)

# test_matches_resolves_intent
#   (pure Python — no niva flow)

# test_by_target_inverts
#   (pure Python — no niva flow)

# test_unknown_keyword_is_empty
#   (pure Python — no niva flow)
