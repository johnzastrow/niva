# ============================================================================
# Companion to tests/test_credentials.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for niva.credentials — the shared secret resolver (env override, else QGIS auth store).

# ── TestGetSecret ───────────────────────────────────────────────

# test_env_var_wins_as_override
#   (pure Python — no niva flow)

# test_falls_back_to_auth_store
#   (pure Python — no niva flow)

# test_none_when_both_absent
#   (pure Python — no niva flow)

# test_empty_env_value_falls_through_to_store
#   (pure Python — no niva flow)

# test_convenience_wrappers
#   (pure Python — no niva flow)

# ── TestFromAuthStore ───────────────────────────────────────────

# test_unknown_kind_is_none
#   (pure Python — no niva flow)

# test_no_qgis_returns_none
#   (pure Python — no niva flow)

# ── TestKeysShared ──────────────────────────────────────────────

# test_authcfg_keys_match_plugin_contract
#   (pure Python — no niva flow)
