# ============================================================================
# Companion to tests/test_output_routing.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Output-routing tests — every verb's text must reach the user the SAME way.

# ── TestReportVerbsRouteIdentically ─────────────────────────────

# test_to_file_writes_report_and_emits_one_status_line
#   (pure Python — no niva flow)

# test_progress_set_streams_report_to_the_dock
#   (pure Python — no niva flow)

# test_no_progress_prints_report_to_stdout
#   (pure Python — no niva flow)

# test_same_text_regardless_of_sink
#   (pure Python — no niva flow)

# ── TestActionVerbsEmitToTheDock ────────────────────────────────

# test_assess_emits_report_path
load a.gpkg | assess to {out}

# test_style_emits_confirmation
load a.gpkg | style save out.qml

# test_metadata_emits_confirmation
load a.gpkg | metadata set title="Roads"

# test_catalog_emits_report_path
#   (pure Python — no niva flow)

# test_notify_emits_target
#   (pure Python — no niva flow)

# ── TestReportVerbValidation ────────────────────────────────────

# test_describe_requires_one_target
#   (pure Python — no niva flow)

# test_report_verbs_reject_unknown_options
#   (pure Python — no niva flow)
