# ============================================================================
# Companion to tests/test_transpile.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Tests for .niva <-> .py transpilation (niva/transpile.py). No QGIS needed —

# ── TestExport ──────────────────────────────────────────────────

# test_export_is_valid_python
load "a.gpkg" | buffer 100m | save "out.gpkg"

# test_export_threads_input_output_and_save
load "a.gpkg" | buffer 100m | run native:centroids ALL_PARTS=true | save "out.gpkg"

# test_multiple_flows_get_distinct_vars_and_keep_explicit_output
#   (pure Python — no niva flow)

# test_export_run_escape_hatch_is_verbatim
#   (pure Python — no niva flow)

# test_export_header_warns_about_reimport_limits
load "a.gpkg" | save "b.gpkg"

# test_export_notes_unconverted_verbs
load "a.gpkg" | assess

# ── TestImport ──────────────────────────────────────────────────

# test_import_linear_processing_run
#   (pure Python — no niva flow)

# test_round_trip_export_then_import
load "a.gpkg" | run native:buffer DISTANCE=5 | run native:centroids ALL_PARTS=true | save "out.gpkg"

# test_edit_exported_py_then_import_picks_up_new_params
load "a.gpkg" | run native:buffer DISTANCE=5 | save "out.gpkg"

# test_add_whole_new_processing_step_then_import
load "a.gpkg" | run native:buffer DISTANCE=5 | save "out.gpkg"

# test_added_param_referencing_a_variable_is_flagged
#   (pure Python — no niva flow)

# test_import_flags_control_flow
#   (pure Python — no niva flow)

# test_import_flags_non_literal_params
#   (pure Python — no niva flow)

# test_import_of_invalid_python_is_reported
#   (pure Python — no niva flow)

# ── TestGeometryTypeErrorMatcher ────────────────────────────────

# test_matches_typed_sink_rejection
#   (pure Python — no niva flow)

# test_ignores_unrelated_errors
#   (pure Python — no niva flow)
