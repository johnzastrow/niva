# ============================================================================
# Companion to tests/test_journal.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# Run-journal tests (planning/08-§2). Mock-backed — no QGIS.

# ── TestJournal ─────────────────────────────────────────────────

# test_writes_both_files
load a.gpkg | buffer 100m | save out.gpkg

# test_pyqgis_echo_in_jsonl_only
load a.gpkg | buffer 100m | run native:centroids ALL_PARTS=true | save out.gpkg

# test_jsonl_header_records_footer
load a.gpkg | buffer 100m | save out.gpkg

# test_jsonl_records_algorithm_for_alias
load a.gpkg | buffer 100m | save out.gpkg

# test_human_log_is_readable_with_full_paths
load a.gpkg | buffer 100m | save out/b.gpkg

# test_one_line_per_operation
load a.gpkg | buffer 100m | save out.gpkg

# test_session_append_accumulates_runs
load a.gpkg | save a.gpkg
load b.gpkg | save b.gpkg

# test_failure_inline_one_line
buffer 100m

# test_footer_reports_elapsed
load a.gpkg | save out.gpkg
# done:.* in .*(ms|s)

# test_timestamps_present
load a.gpkg | save out.gpkg

# test_failure_is_recorded
buffer 100m

# ── TestRunMetadata ─────────────────────────────────────────────

# test_log_header_carries_niva_version
load a.gpkg | buffer 100m | save out.gpkg

# test_run_emits_version_and_start_finish
load a.gpkg | save out.gpkg

# test_record_note_appears_in_both_files
#   (pure Python — no niva flow)
