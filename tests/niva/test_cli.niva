# ============================================================================
# Companion to tests/test_cli.py — what each test exercises, in niva form.
# AUTO-GENERATED by scripts/gen_test_niva.py — do not edit by hand.
# Regenerate:  python scripts/gen_test_niva.py   (CI fails if this is stale)
# Flows lifted from f-strings keep their {python} placeholders — illustrative,
# not directly runnable.
# ============================================================================
#
# CLI argument routing (`niva.cli.main._read_source`) — no QGIS.

# ── TestReadSource ──────────────────────────────────────────────

# test_run_subcommand_reads_file
#   (pure Python — no niva flow)

# test_lone_niva_file_runs_without_run
#   (pure Python — no niva flow)

# test_missing_niva_file_is_a_clear_error_not_inline
#   (pure Python — no niva flow)

# test_inline_flow_is_unchanged
#   (pure Python — no niva flow)

# test_single_quoted_inline_flow_unchanged
load x.gpkg | save y.gpkg
